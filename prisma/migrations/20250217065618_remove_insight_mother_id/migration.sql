/*
  Warnings:

  - You are about to drop the column `insightMotherId` on the `files` table. All the data in the column will be lost.

*/
-- AlterTable
ALTER TABLE `files` DROP COLUMN `insightMotherId`;
